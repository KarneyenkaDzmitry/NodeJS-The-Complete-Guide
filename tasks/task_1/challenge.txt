Task description.

1. Spin up a Node.js-driven server (on port 3000);

2. Handle two Routes: "/" and "/users";
 - Retrive some greeting text on "/";
 - Retrive a list of dummy users on "/users";

3. Add a form with a "username" <input> to the "/" page
 and submit a POST reqest to "/create-user" upon a button click;

4. Add the "/create-user" route and parse the incoming data (i.e. the username)
 and simplify log it to the console. 