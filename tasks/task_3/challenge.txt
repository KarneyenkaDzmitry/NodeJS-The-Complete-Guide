Task description.

1. Create a npm project and install Express.js (Nodemon if you want);

2. Create an Express.js app which The server should serve two or more HTML files for "/" and "/users";

3. Add some static (.js or .css) files to your project that should be required by at least one of your HTML files.